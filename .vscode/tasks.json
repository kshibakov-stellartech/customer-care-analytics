{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "AWS: Login (aws-login)",
      "type": "shell",
      "command": "zsh",
      "args": [
        "-lic",
        "if command -v aws-login >/dev/null 2>&1; then aws-login; else aws sso login --profile ${input:awsProfile}; fi"
      ],
      "problemMatcher": []
    }
  ],
  "inputs": [
    {
      "id": "awsProfile",
      "type": "promptString",
      "description": "AWS profile",
      "default": "cc-analytics"
    }
  ]
}
